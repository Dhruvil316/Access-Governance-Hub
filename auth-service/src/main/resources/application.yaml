spring:
  application:
    name: auth-service

  config:
    import: optional:configserver:http://localhost:8888

# control the database seeder
  data:
    redis:
      repositories:
        enabled: false

app:
  seed:
    enabled: false